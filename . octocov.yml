# generated by octocov init
coverage:
  if: true
  acceptable: 90%
testExecutionTime:
  if: true
  acceptable: 10 min
diff:
  datastores:
    - artifact://${GITHUB_REPOSITORY}
comment:
  if: is_pull_request
summary:
  if: true
report:
  if: is_default_branch
  datastores:
    - artifact://${GITHUB_REPOSITORY}
