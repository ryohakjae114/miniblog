%h1.fs-2= t('profile.edit.title')

= simple_form_for(@user, html: { method: :put, class: 'mt-5' }) do |f|
  = f.input :introduction, disabled: @user == current_user ? '' : true
  = f.input :external_blog_url, disabled: @user == current_user ? '' : true

  = f.button :submit
